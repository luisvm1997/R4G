@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Datos personales";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Tu cuenta contiene datos personales que nos has proporcionado. Aquí puedes descargarlos o eliminarlos.</p>
        <p>
            <strong>Al eliminar estos datos se borrará tu cuenta de forma permanente y no podrás recuperarla.</strong>
        </p>
        <form id="download-data" asp-page="DescargarPersonalData" method="post">
            <button class="btn btn-primary" type="submit">Descargar</button>
        </form>
        <p>
            <a id="delete" asp-page="EliminarPersonalData" class="btn btn-danger">Eliminar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
